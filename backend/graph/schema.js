/**
 * graph/schema.js — SQLite schema init (documents table is created in shared.js).
 */

import { logger } from '../utils/logger.js';

export function initSchema() {
  logger.info('Schema ready');
}
